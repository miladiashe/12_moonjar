#include <stdio.h>
#include <stdlib.h>
#include <string.h> 

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	char str[100] = "help me my code is not working";
	
	
	printf("문자열\"%s\" 의 길이 %i", str, strlen(str));
	return 0;
}
